<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <div class="pirate">
      <h1><%= "#{@pirate.name}: #{@pirate.weight} pounds, #{@pirate.height} tall" %></h1>
    </div>

    <h2><%= "#{@ships[0].name}: #{@ships[0].type}, #{@ships[0].booty}" %></h2>
    <h2><%= "#{@ships[1].name}: #{@ships[1].type}, #{@ships[1].booty}" %></h2>
  </body>
</html>
